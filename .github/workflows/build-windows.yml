name: Build Windows 10 package

on:
  push:
    branches: [main, master]
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-2022
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install PySide6==6.7.3 pyinstaller==6.11.1

      - name: Build executable
        run: >-
          pyinstaller --noconfirm --clean --windowed
          --name MakroUploader
          --collect-all PySide6
          app.py

      - name: Add user documentation
        shell: pwsh
        run: Copy-Item README.md dist/MakroUploader/README.md

      - name: Create portable zip
        shell: pwsh
        run: Compress-Archive -Path dist/MakroUploader -DestinationPath dist/MakroUploader-Windows10.zip

      - name: Upload Windows package
        uses: actions/upload-artifact@v4
        with:
          name: MakroUploader-Windows10
          path: dist/MakroUploader-Windows10.zip
          if-no-files-found: error

      - name: Publish GitHub Release package
        if: startsWith(github.ref, 'refs/tags/')
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh release create $env:GITHUB_REF_NAME
          dist/MakroUploader-Windows10.zip
          --repo $env:GITHUB_REPOSITORY
          --title "MakroUploader $env:GITHUB_REF_NAME"
          --notes "Windows 10 portable package."
